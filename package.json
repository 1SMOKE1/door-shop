{
  "name": "door_shop",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npm cache clean --force && node index.js",
    "prebuild": "cd backend && npm cache clean --force && npm run build",
    "build": "cd frontend && npm link @angular/cli && npm run build"
  },
  "engines": {
    "node": "v14.18.0",
    "npm": "8.19.1"
  }
}